<form method="post" action="{{ path('chat_room_delete', {'id': chat_room.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chat_room.id) }}">
    <button class="btn">Delete</button>
</form>
